    # phase = 'base'
    # fc = phaseFC_extract(phase)
    # fc_visual_save(fc = fc, savefile_prefix = savefile_fcgraph_prefix + '_' + phase)


    # phase = 'reach'
    # fc = phaseFC_extract(phase)
    # fc_visual_save(fc = fc, savefile_prefix = savefile_fcgraph_prefix + '_' + phase)